<%= form_for(@comment) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this reaction from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :commenttext %><br />
    <%= f.text_area :commenttext %>
  </div>
  <%= f.hidden_field :newsitem_id %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
