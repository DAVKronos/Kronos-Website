%h1 Alle Leden

%ul{:class => "users"}

  - @users.each do |user|
  
    %li
      = link_to user.name, user

      = link_to 'verwijder', user, :method => :delete, :confirm => "Weet je het zeker?", :title => "Verwijder #{user.name}"