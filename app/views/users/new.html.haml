%h1= t(:sign_up).capitalize

= form_for @user, :validate => true, :html => { :multipart => true }  do |f|

  -if can? :new, User    
    = render 'admin_fields', :f => f
  -else
    = render 'fields', :f => f


  #actions
  =f.submit :toevoegen
