%h1= t(:sign_up).capitalize

= form_for @user, :html => { :multipart => true }  do |f|

  -if current_user.has_role?(:admin)    
    = render 'admin_fields', :f => f
  -else
    = render 'fields', :f => f


  #actions
  =f.submit :toevoegen
