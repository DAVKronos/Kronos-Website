.row
  .col-lg-12
    %h1= t(:new_mailinglist).capitalize

.row
  .col-lg-6

    = nested_form_for @mailinglist, :validate => true, :html => { :multipart => true }  do |f|

      = render 'fields', :f => f


      #actions
        =f.submit :toevoegen, :class => 'btn btn-default'
