%h1= t :all_mailinglists

- @mailinglists.each do |mailinglist|

  = render "mailinglist", :mailinglist => mailinglist
  = link_to raw("<span class='glyphicon glyphicon-remove'></span> Delete"), mailinglist ,:class => 'btn btn-mini',
   confirm: 'Weet je het zeker?', method: :delete if can? :destroy, mailinglist
