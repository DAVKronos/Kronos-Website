.eventname-edit
  = event.eventtype.name
  -if can? :destroy, event
    =link_to raw("<i class='glyphicon white glyphicon-remove'></i>"), event, confirm: 'Weet je het zeker?', method: :delete
%ul
  - event.results.each do |result|
    %li
      = result.username
      prestatie:
      = result.result
      - if result.place
        = '(' + result.place.to_s + 'e)'
      score:
      = number_to_human result.calculatedResult, :precision => 2, :significant => false
      = result.event.eventtype.calculated_unit
      = link_to "verwijder", result, confirm: 'Weet je het zeker?', method: :delete if can? :destroy, result