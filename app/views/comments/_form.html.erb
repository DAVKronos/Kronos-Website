<%= form_for(@comment) do |f| %>
  <div class="field">
    <%= f.label :commenttext %><br />
    <%= f.text_area :commenttext %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
