%table
  -@users.in_groups_of(3, false) do |users|
    %tr
      -users.each do |user|
        %td
          = user.name
          %br
          = user.address
          %br
          = user.postalcode
          = user.city
 
  