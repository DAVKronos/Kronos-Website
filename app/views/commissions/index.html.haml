%h1 Alle Commissies

%ul{:class => "commissions"}

  - @commissions.each do |commission|
  
    %li
      = link_to commission.name, commission
      - if current_user.admin?
        = link_to "delete", commission, :method => :delete, :confirm => "You sure?", :title => "Delete #{commission.name}"
