<%= form_for(@apps_exception) do |f| %>
  <% if @apps_exception.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@apps_exception.errors.count, "error") %> prohibited this apps_exception from being saved:</h2>

      <ul>
      <% @apps_exception.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :way %><br />
    <%= f.check_box :way %>
  </div>
  <div class="field">
    <%= f.label :group_email %><br />
    <%= f.text_field :group_email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
