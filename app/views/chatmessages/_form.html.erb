<%= form_for(@chatmessage) do |f| %>
  <% if @chatmessage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chatmessage.errors.count, "error") %> prohibited this chatmessage from being saved:</h2>
      <ul>
      <% @chatmessage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :by %><br />
    <%= f.text_field :by %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :message %><br />
    <%= raw f.text_area(:message).gsub("\n", '&#x000A;').gsub("\r",'') %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
