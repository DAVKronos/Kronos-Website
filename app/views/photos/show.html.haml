.carousel{:id => "myCarousel"}
  .carousel-inner
    - @photo.photoalbum.photos.each do |photo|
      %div{:class => "item #{@photo == photo ? 'active' : '' }"}

        = image_tag photo.photo.url(:normal)
        .carousel-caption
          %h4
            = photo.caption
          - show_to :admin do
            = link_to 'verwijder', photo, :method => :delete, :confirm => "Weet je het zeker?", :class => 'btn'
        
      
      
      
  <!-- Carousel nav -->
  <a class="carousel-control left" href="#myCarousel" data-slide="prev">&lsaquo;</a>
  <a class="carousel-control right" href="#myCarousel" data-slide="next">&rsaquo;</a>
- @photo.reactions.each do |reactie|
  %p
    = reactie.reactiontext

= render 'reactions_form'

