[supervisord]
nodaemon=true

[program:railsapp]
command=/bin/bash -c "cd /home/rails/webapp; bundle exec rails s -p 3000 -b '0.0.0.0' -e production"

[program:worker]
command=/bin/bash -c "RAILS_ENV=production cd /home/rails/webapp; bundle exec rake jobs:work"